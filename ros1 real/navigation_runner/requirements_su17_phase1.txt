# Install the CPU-only PyTorch 2.0.1 wheel separately before this file.
# TensorDict and TorchRL must then be installed from this repository's
# isaac-training/third_party sources with --no-deps (both report version 0.4.0).
numpy==1.24.4
hydra-core==1.3.2
einops==0.7.0
tomli==2.0.1
cloudpickle==2.2.1
packaging==23.2
